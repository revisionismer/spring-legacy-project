<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.nexchal.board.mappers.reply.ReplyMapper">
	
	<insert id="insertReply">
		<selectKey order="AFTER" keyProperty="rno" resultType="long">
			SELECT LAST_INSERT_ID()
		</selectKey>
		
		INSERT INTO tb_reply(
			bno,
			title,
			content,
			writer,
			deleteYn
		)
		VALUES(
			#{bno},
			#{title},
			#{content},
			#{writer},
			false
		)
		
	</insert>
	
	<select id="findReplyOne" parameterType="Long" resultType="ReplyVO">
		SELECT
			*
		FROM
			tb_reply
		WHERE
			rno = #{rno}
	</select>
	
	<update id="updateReply" parameterType="ReplyVO">
		UPDATE
			TB_REPLY
		SET
			title = #{title},
			content = #{content},
			deleteYn = #{deleteYn},
			updateDate = now()
		WHERE
			rno = #{rno}
	</update>
	
	<select id="findAllReplyList" resultType="ReplyVO">
		SELECT
			*
		FROM
			TB_REPLY
		WHERE
			bno = #{bno} AND rno > 0
		LIMIT
			#{cri.skip}, #{cri.amount}
	</select>
	
	<select id="getTotalCount" resultType="int">
		SELECT
			COUNT(rno)
		FROM
			TB_REPLY
		WHERE
			bno = #{bno}
	</select>
	
</mapper>