<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring-1.2.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">
	
	<!-- Root Context: defines shared resources visible to all other web components -->
	
	<!-- 비즈니스 로직(service, dao, mybatis, DB)과 관련된 설정은 root-context.xml에서 처리 -->
	
	<!-- 2-1. hikariCP 데이터베이스 설정 : hikariConfig -->
	<bean id="hikariConfig" class="com.zaxxer.hikari.HikariConfig">
		<property name="driverClassName" value="org.mariadb.jdbc.Driver" />
		<property name="jdbcUrl" value="jdbc:mariadb://localhost:3306/nexchal" />
		<property name="username" value="root" />
		<property name="password" value="1234" />
		<property name="connectionTimeout" value="30000" />
		<property name="autoCommit" value="false"/>
		<!-- <property name="minimumIdle" value="2" />  -->
	</bean>
	
	<!-- 2-2. hikariCP 데이터베이스 설정 : DataSource -->
	<bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource" destroy-method="close">
		<!-- 2-4. 2-1에서 만든 hikariConfig 정보를 hikariDataSource 생성자에 등록 -->
		<constructor-arg ref="hikariConfig" />
		
	</bean>
	
	<!-- 3-1. mybatis 설정 -->
	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<property name="configLocation" value="classpath:mybatis-config.xml" />
		<property name="mapperLocations" value="classpath:mappers/*/*.xml"/>
	</bean>
	
	<!-- 3-2. mybatis-spring scan 설정 1 : sample -->
	<mybatis-spring:scan base-package="com.nexchal.sample.mappers"/>
	
	<!-- 3-3. mybatis-spring scan 설정 2 : board -->
	<mybatis-spring:scan base-package="com.nexchal.board.mappers"/>
	
	<!-- 3-4. mybatis-spring scan 설정 3 : user -->
	<mybatis-spring:scan base-package="com.nexchal.user.mappers"/>
	
	<!-- 4-1. component-scan 설정 1(주의: @Service는 @Component의 일종이므로 component-scan으로 등록해줘야 한다.) -->
	<context:component-scan base-package="com.nexchal.board.service"/>
	
	<!-- 4-2. component-scan 설정 2 : aop 트랜잭션 테스트를 할거기 때문에 테스트 진행할 sample 서비스 경로도 컴포넌트 스캔 경로로 등록.-->
	<context:component-scan base-package="com.nexchal.sample.service"/>
	
	<!-- 4-3. component-scan 설정 3 : User 관련 서비스 경로 컴포넌트 스캔 대상에 등록 -->
	<context:component-scan base-package="com.nexchal.user.service"/>
	
	<!-- 5-1. 트랜잭션 설정 1-->
	<bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="dataSource" />
	</bean>
	
	<!-- 5-2. 트랜잭션 설정 2 : 트랜잭션 어노테이션 활성화(주의 : 트랜잭션으로 묶어서 실행할 경우 서비스단에서 한꺼번에 묶고 컨트롤러에서 실행하야 동작함. 따로 따로 만들어서 컨트롤러에서 실행하면 둘다 롤백되는게 아니라 하나씩 적용됨. -->
	<tx:annotation-driven />
	
</beans>
